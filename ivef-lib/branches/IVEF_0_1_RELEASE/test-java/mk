#
# cleanup
#
#rm -Rf classes/*
mkdir -p classes
mkdir -p target

# compile the code
#

javac -Xlint -source 1.4 -classpath classes:../ivef-java/ivef/ivef.jar -d classes src/*java

cd classes
#
# create a complete jar and a small jar
#

jar -cMf ../target/testjava.jar *
