#include "convert.h"


Convert::Convert() {
}

IVEF2msg_loginrequest Convert::convert(IVEF1msg_loginrequest *msg) {

    IVEF2msg_loginrequest newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_loginresponse Convert::convert(IVEF1msg_loginresponse *msg) {

    IVEF2msg_loginresponse newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_logout Convert::convert(IVEF1msg_logout *msg) {

    IVEF2msg_logout newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_ping Convert::convert(IVEF1msg_ping *msg) {

    IVEF2msg_ping newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_pong Convert::convert(IVEF1msg_pong *msg) {

    IVEF2msg_pong newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_serverstatus Convert::convert(IVEF1msg_serverstatus *msg) {

    IVEF2msg_serverstatus newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF2msg_servicerequest Convert::convert(IVEF1msg_servicerequest *msg) {

    IVEF2msg_servicerequest newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_loginrequest Convert::convert(IVEF2msg_loginrequest *msg) {

    IVEF1msg_loginrequest newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_loginresponse Convert::convert(IVEF2msg_loginresponse *msg) {

    IVEF1msg_loginresponse newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_logout Convert::convert(IVEF2msg_logout *msg) {

    IVEF1msg_logout newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_ping Convert::convert(IVEF2msg_ping *msg) {

    IVEF1msg_ping newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_pong Convert::convert(IVEF2msg_pong *msg) {

    IVEF1msg_pong newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_serverstatus Convert::convert(IVEF2msg_serverstatus *msg) {

    IVEF1msg_serverstatus newMsg;

    // perform the actual conversion

    return newMsg;
}

IVEF1msg_servicerequest Convert::convert(IVEF2msg_servicerequest *msg) {

    IVEF1msg_servicerequest newMsg;

    // perform the actual conversion

    return newMsg;
}

